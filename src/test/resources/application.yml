spring:
  application:
    name: mstruckaccount

tradeaccount:
  endpoint: http://localhost:9000

server:
  port: 8000

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics

